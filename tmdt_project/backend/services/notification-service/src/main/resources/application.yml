spring:
  application:
    name: notification-service

server:
  port: 8086

management:
  endpoints:
    web:
      exposure:
        include: health,info

# spring:
#   kafka:
#     bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
